extends ../base
include ../mixins/item

block content 
	form(method="GET")
		input(placeholder="Search by title",name="keyword", type="text")
		input(type="submit", value="Search now")
	dev 
		a(href="board/upload") Writing &rarr;
	ul 
		each item in items
			+item(item)
		else 
			li Sorry nothing found.
	ul 